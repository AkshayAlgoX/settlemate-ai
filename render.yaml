services:
  - type: web
    name: settlemate-ai
    env: node
    plan: free
    buildCommand: npm ci && npm run build:prod
    startCommand: npm run start
    healthCheckPath: /api/v1/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PRISMA_TARGET_PROVIDER
        value: postgresql
      - key: DATABASE_URL
        sync: false
      - key: AUTH_SECRET
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: WEBHOOK_SECRET
        sync: false
      - key: WEBHOOK_SHARED_SECRET
        sync: false
      - key: METRICS_TOKEN
        sync: false
